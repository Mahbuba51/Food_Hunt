<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Profile</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            background-image: url("/images (8).jpeg");
            background-repeat: no-repeat;
            background-size: cover;
        }

        .container {
            margin-top: 50px;
        }

        .profile-card {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="profile-card">
                    <h1 class="mb-4">Customer Profile</h1>
                    <div class="mb-3">
                        <strong>Name:</strong> <%= customer.name %>
                    </div>
                    <div class="mb-3">
                        <strong>Contact Number:</strong> <%= customer.contact_no %>
                    </div>
                    <div class="mb-3">
                        <strong>Location:</strong> <%= customer.location %>
                    </div>
                    <div class="mb-3">
                        <strong>Email:</strong> <%= customer.email %>
                    </div>
                    
                </div>
            </div>
        </div>
    </div>
</body>

</html> -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Profile</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            background-image: url("/images (8).jpeg");
            background-repeat: no-repeat;
            background-size: cover;
        }

        .container {
            margin-top: 50px;
        }

        .profile-card {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .update-form {
            display: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="profile-card">
                    <h1 class="mb-4">Customer Profile</h1>
                    <div class="mb-3">
                        <strong>Name:</strong> <%= customer.name %>
                        <button class="btn btn-primary update-btn" data-target="name">Update</button>
                    </div>
                    <div class="mb-3">
                        <strong>Contact Number:</strong> <%= customer.contact_no %>
                        <button class="btn btn-primary update-btn" data-target="contact-no">Update</button>
                    </div>
                    <div class="mb-3">
                        <strong>Location:</strong> <%= customer.location %>
                        <button class="btn btn-primary update-btn" data-target="location">Update</button>
                    </div>
                    <div class="mb-3">
                        <strong>Email:</strong> <%= customer.email %>
                        <button class="btn btn-primary update-btn" data-target="email">Update</button>
                    </div>
                    <!-- Add any other customer details you want to display -->

                    <!-- Update Profile Forms -->
                    <form class="update-form" id="name-form" action="/restaurant/update-name" method="POST">
                        <input type="text" name="name" placeholder="Enter new name">
                        <input type="hidden" name="email" value="<%= customer.email %>">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </form>

                    <form class="update-form" id="contact-no-form" action="/restaurant/update-contact-no" method="POST">
                        <input type="text" name="contact_no" placeholder="Enter new contact number">
                        <input type="hidden" name="email" value="<%= customer.email %>">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </form>

                    <form class="update-form" id="location-form" action="/restaurant/update-location" method="POST">
                        <input type="text" name="location" placeholder="Enter new location">
                        <input type="hidden" name="email" value="<%= customer.email%>">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </form>

                    <form class="update-form" id="email-form" action="/restaurant/update-email" method="POST">
                        <input type="email" name="new_email" placeholder="Enter new email">
                        <input type="hidden" name="email" value="<%= customer.email%>">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Script to toggle update forms
        const updateButtons = document.querySelectorAll('.update-btn');
        updateButtons.forEach(button => {
            button.addEventListener('click', () => {
                const target = button.dataset.target;
                const form = document.getElementById(`${target}-form`);
                if (form) {
                    // Hide all forms first
                    document.querySelectorAll('.update-form').forEach(form => {
                        form.style.display = 'none';
                    });
                    // Show the corresponding form
                    form.style.display = 'block';
                }
            });
        });
    </script>
</body>

</html>
